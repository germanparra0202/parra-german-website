[project]
name = "parra-german-website"
version = "0.1.0"
dependencies = [
    "fastapi>=0.111.0",
    "uvicorn>=0.30.1",
    "pydantic[email]>=2.7.4",
    "python-multipart>=0.0.9"
]
requires-python = ">=3.12"

[tool.vercel]
entrypoint = "backend.main:app"
